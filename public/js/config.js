// public/js/config.js
// if injecting a config.js when deploying, can replace this url with the base socket url in prod
// other option is to directly replace in index.html before deploying (see commented line in index.html)
window.ENV = {
    BASE_SOCKET_URL: "http://localhost:3000" 
};
  